// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    classpath('com.android.tools.build:gradle')
    classpath('com.facebook.react:react-native-gradle-plugin')
    classpath('org.jetbrains.kotlin:kotlin-gradle-plugin')
    // RÉ-AJOUTÉ après la régén SDK 54 (prebuild ne le remet pas car app.json ne
    // déclare pas le plugin @react-native-firebase) : sans ce plugin, le
    // google-services.json n'est pas traité → "Default FirebaseApp is not
    // initialized" → écran blanc au démarrage Android.
    classpath('com.google.gms:google-services:4.4.2')
  }
}

allprojects {
  repositories {
    google()
    mavenCentral()
    maven { url 'https://www.jitpack.io' }
  }
}

apply plugin: "expo-root-project"
apply plugin: "com.facebook.react.rootproject"
